# frontend

React + Vite PWA dashboard for server-app. See the [repo root README](../README.md)
for the full architecture and deployment instructions.

```bash
npm install
npm run dev      # dev server on :5173, proxies /api to localhost:4000
npm run build    # production build -> dist/ (served by the backend)
```
